services:
  api:
    build: .
    ports:
      - "10000:10000"
    environment:
      - DB_ENCRYPTION_KEY=${DB_ENCRYPTION_KEY}
      - CORS_ORIGINS=${CORS_ORIGINS:-http://localhost:10001}
      - DEV=${DEV:-}
      - LOG_FILE=${LOG_FILE:-/app/logs/provinci.log}
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - terraform-state:/app/terraform
    restart: unless-stopped

volumes:
  terraform-state:
